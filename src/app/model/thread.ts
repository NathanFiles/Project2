export class thread {
    t_id: number;
    num_posts: number;
    active: number;

    constructor(t_id: number, num_posts:number, active: number) {
        this.t_id = t_id;
        this.num_posts = num_posts;
        this.active = active;
    }

}

